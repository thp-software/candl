export enum CandlXLabelType {
  /**
   * Label every minute
   */
  LabelMinute = 0,
  /**
   * Label every 5 minutes
   */
  Label5Minutes = 1,
  /**
   * Label every 10 minutes
   */
  Label10Minutes = 2,
  /**
   * Label every 15 minutes
   */
  Label15Minutes = 3,
  /**
   * Label every 20 minutes
   */
  Label20Minutes = 4,
  /**
   * Label every 30 minutes
   */
  Label30Minutes = 5,
  /**
   * Label every hour
   */
  LabelHour = 6,
  /**
   * Label every 2 hours
   */
  Label2Hours = 7,
  /**
   * Label every 4 hours
   */
  Label4Hours = 8,
  /**
   * Label every 8 hours
   */
  Label8Hours = 9,
  /**
   * Label every 12 hours
   */
  Label12Hours = 10,
  /**
   * Label every day
   */
  LabelDay = 11,
  /**
   * Label every 2 days
   */
  Label2Days = 12,
  /**
   * Label every 4 days
   */
  Label4Days = 13,
  /**
   * Label every 8 days
   */
  Label8Days = 14,
  /**
   * Label every week
   */
  LabelWeek = 15,
  /**
   * Label every month
   */
  LabelMonth = 16,
  /**
   * Label every year
   */
  LabelYear = 17,
}
