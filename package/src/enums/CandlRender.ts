export enum CandlRenderMode {
  /**
   * Draw candles with their shadows
   */
  CandleAndShadow = 0,
  /**
   * Draw candles only
   */
  CandleOnly = 1,
  /**
   * Draw a line
   */
  Line = 2,
  /**
   * TODO Draw minified candles (vertical lines)
   */
  MinifiedCandle = 3,

  Area = 4,
}
